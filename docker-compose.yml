version: "3"

services:

  backend:
    build: ./server
    ports:
      - "5000:5000"

  frontend:
    build: ./client
    ports:
      - "5173:5173"

  mongodb:
    image: mongo
    ports:
      - "27017:27017"